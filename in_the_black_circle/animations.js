
// const fun_svg = document.getElementById('fun_svg');
// const [...blocks] = document.getElementById('fun_colour_blocks').children;
// const [...gradients] = document.getElementById('fun_gradients').children;

// const [...rects] = blocks.filter(b => (b.id).startsWith('rect_'));
// const [...circles] = blocks.filter(b => (b.id).startsWith('circle_'));
// const [...shapes] = blocks.filter(b => (b.id).startsWith('shape_'));
// const [...polygons] = blocks.filter(b => (b.id).startsWith('polygon_'));
// const [...lines] = blocks.filter(b => (b.id).startsWith('line_'));
// const [...paths] = blocks.filter(b => (b.id).startsWith('path_'));




// // console.log(blocks);

// blocks.forEach(block => {
//   const [...childs] = block.children;
//   const [...child_rects] = childs.filter(b => (b.id).startsWith('rect_'));
//   const [...child_circles] = childs.filter(b => (b.id).startsWith('circle_'));
//   const [...child_polygons] = childs.filter(b => (b.id).startsWith('polygon_'));
//   const [...child_lines] = childs.filter(b => (b.id).startsWith('line_'));
//   const [...child_paths] = childs.filter(b => (b.id).startsWith('path_'));
//   if (child_rects.length > 0) { child_rects.forEach(ch => rects.push(ch)) }
//   if (child_circles.length > 0) { child_circles.forEach(ch => circles.push(ch)) }
//   if (child_polygons.length > 0) { child_polygons.forEach(ch => polygons.push(ch)) }
//   if (child_lines.length > 0) { child_lines.forEach(ch => lines.push(ch)) }
//   if (child_paths.length > 0) { child_paths.forEach(ch => paths.push(ch)) }
// });
// console.log(rects);
// console.log(circles);
// console.log(shapes);
// console.log(polygons);
// console.log(lines);
// console.log(paths);


// // const urlParams = new URLSearchParams(window.location.search);
// console.log(`There are a total of ${blocks.length} colour blocks.`);
// console.log(`And there are ${gradients.length} unique gradients.`);
// console.log(`And there are ${rects.length} rectangles.`);
// console.log(`And there are ${circles.length} circles.`);
// console.log(`And there are ${shapes.length} shapes.`);
// console.log(`And there are ${polygons.length} polygons.`);
// console.log(`And there are ${lines.length} lines.`);
// console.log(`And there are ${paths.length} paths.`);




